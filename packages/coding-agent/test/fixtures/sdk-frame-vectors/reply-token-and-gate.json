{
  "$schema": "sdk-frame-vectors/v1",
  "kind": "record",
  "name": "reply token and workflow gate shape",
  "frames": [
    { "type": "reply", "id": "ask-1", "answer": 0, "token": "discovery-token-required" },
    { "type": "action_needed", "kind": "workflow_gate", "id": "gate-1", "sessionId": "session-1", "stage": "deep-interview", "schema": { "type": "string", "enum": ["continue"] } }
  ],
  "expectations": { "replyTokenRequired": true, "type": "reply" }
}
