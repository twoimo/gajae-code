<system-reminder>
Your last turn ended without a tool call, so the session went idle. This is reminder {{retryCount}} of {{maxRetries}}.

Resume the assignment by calling the next required tool. Only when the work is complete, or a concrete blocker prevents progress, use `yield` according to the canonical CHILD completion contract in your system prompt. Do not stop with text only.
</system-reminder>
